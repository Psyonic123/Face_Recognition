To run for yourself:

1) Create two folders in .py directory, known_faces, and unknown_faces
2) Create user folders in known_faces directory such as "Nathan", "User2", etc.
3) Populate known_faces with images of the user. This is the training set
4) Populate unknown_faces with images of anyone. The program will create a box over their face if match found